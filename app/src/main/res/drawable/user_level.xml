<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/indefinite_gray"
                android:centerColor="@color/indefinite_gray"
                android:centerY="1.0"
                android:endColor="@color/indefinite_gray"
                android:angle="270"/>

            <corners android:bottomRightRadius="20dp"
                android:topRightRadius="20dp" />

        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <scale android:scaleWidth="100%">
            <shape>
                <gradient
                    android:startColor="@color/colorSecondary"
                    android:centerColor="@color/colorSecondary"
                    android:centerY="1.0"
                    android:endColor="@color/colorSecondary"
                    android:angle="270"/>

                <corners android:bottomRightRadius="20dp"
                    android:topRightRadius="20dp" />

            </shape>
        </scale>
    </item>
</layer-list>